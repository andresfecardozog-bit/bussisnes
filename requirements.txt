# Runtime deps (imagen Docker + Railway). Ver requirements-dev.txt para
# dependencias de testing y generacion de recursos.

pandas>=2.0
openpyxl>=3.1
Pillow>=10.0
python-dateutil>=2.8
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
httpx>=0.27
pydantic>=2

# Supabase Storage (Fase 7E). Se activa via SUPABASE_URL + SUPABASE_SERVICE_KEY.
supabase>=2.0
